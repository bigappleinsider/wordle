import styled from "styled-components";

const StyledMessageButton = styled.button`
	padding: 8px 16px;
	font-size: 16px;
	font-weight: bold;
	border: none;
	background-color: #000;
	color: #fff;
	cursor: pointer;
	border-radius: 4px;
	margin-top: 20px;
`;

export default StyledMessageButton;
